1.刚开始完全没有思路，看了几篇博客，后来大佬一讲就明白了，文件（目录）就是一堆二进制，你想把它怎么解释就怎么解释
2.确定思路文件=文件结构（struct+内容），就开始写代码，（basecore的字符串处理花了我大半天时间，77777），4个函数，
  每个函数两个参数，输入文件，输出文件。写到解包/打包目录就懵逼了，没法调用递归。。。
3.后来改成3+4的方式，但是4个函数里面用文件路径传参，运行和打包文件都没问题，但解包就出现乱码（文件结构的内容
）。虽然考虑到了文件读取mydata后，读取指针的偏移量，但还是太年轻，用文件作为参数，调用递归时，又从新打开，又从
文件头开始读！！！！！
4.后来用FileStream文件流作为参数，就ok了
5.里面有很多bug，且没有错误处理